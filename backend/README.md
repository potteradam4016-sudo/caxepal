# SCNU PICK 백엔드

프론트엔드 없이 독립 실행되는 FastAPI 백엔드입니다. 이 폴더 전체를 팀 저장소의 `backend/`에 넣습니다.
기존 `frontend/`, 루트 `docs/`, `.github/` 파일을 덮어쓰지 않습니다.

> 이 구현의 기능 기준은 첨부 저장소 `docs/product-scope.md`입니다.
> 추천 / 신규 / **찜 기반 월간 캘린더**, 아이디·비밀번호 인증, 세 가지 수집 출처를 반영했습니다.
> 과거 기획서의 네 번째 출처·별도 마감 메뉴·Supabase Auth를 그대로 가져온 버전이 아닙니다.
> API 경로·관심사 사전·추천 가중치·인증 방식·운영 서비스는 **구현 제안값**이며 팀 합의가 필요합니다.

## 1. 처음 시작하기

Python 3.11 이상이 필요합니다. 이번 인증 전환은 Windows / Python 3.12.14에서 검증했습니다.
Windows에서는 Python이 설치되어 있고 `python --version`이 정상 출력되는지 먼저 확인합니다.

압축을 푼 `backend` 폴더에서:

```powershell
python start.py
```

Windows에서는 `START.cmd`를 두 번 클릭해도 같은 준비 절차가 실행됩니다.
macOS/Linux에서 `python` 명령이 없다면 `python3 start.py`를 사용합니다.
첫 실행은 인터넷을 통해 의존 패키지를 설치합니다. 설치가 실패하면 터미널 오류를 확인해야 합니다.

자동으로 하는 일은 `.env` 및 무작위 비밀키 생성 → `.venv` 생성 → 패키지 설치 → DB 마이그레이션
→ 출처·관심사 사전 초기화 → API와 작업 처리기 실행입니다.
이미 존재하는 `.env`와 사용자 DB는 초기화하거나 삭제하지 않습니다.

| 확인 대상 | 주소 |
| --- | --- |
| 실행 상태 | `http://localhost:3104/health` |
| API 시험 화면 | `http://localhost:3104/docs` |
| API JSON 명세 | `http://localhost:3104/openapi.json` |
| 백엔드 식별 정보 | `http://localhost:3104/` |

`/`에서 JSON이 보이는 것이 정상입니다. 사용자 웹 화면은 없습니다.
`/health`는 `{"status":"ok","database":"ok","frontend_included":false}`를 반환합니다.
종료는 실행한 터미널에서 `Ctrl+C`입니다.

**처음 공지 목록이 비어 있는 것은 정상입니다. 가짜 공지·테스트 계정·기본 관리자 계정을 넣지 않았습니다.**
학교 공지는 허가 조건 확인 및 수집 설정 후 별도로 수집합니다.

## 2. 구현한 기능

| 영역 | 포함 내용 |
| --- | --- |
| 계정 | 아이디·비밀번호 가입, 로그인, 현재 사용자, 로그아웃·전체 로그아웃, 현재 비밀번호를 확인하는 변경, 탈퇴 |
| 학적·관심사 | 본인 학과·학년·학적, 분야/활동 유형 분리, 관심사 저장, 온보딩 여부, 수정 버전 충돌 확인 |
| 공지 | 출처·카테고리·검색·기간·마감 필터, 원문 등록일 정렬, 상세·첨부 메타데이터 |
| 추천 | 학적·관심사 적합도, 실제 점수의 이유, 항목별 점수, 신청 마감·명확한 자격 불일치 제외 |
| 일정·찜 | 사용자별 찜, 신청/행사 일정 분리, 월과 겹치는 기간, 지난 신청/향후 행사 유지, 일정 미정 |
| 수집 | 대표/SW/AI 세 게시판 어댑터, 요청 간격, 제한된 재시도, robots 확인, 중복 방지, 원문 변경 감지 |
| 분석 | 규칙 기반 추출, 선택적 OpenAI 구조화 요청, 근거·날짜 검증, 분석 실패 표시, 관리자 수동 검수 |
| 운영 | DB 마이그레이션, 영속 수집 작업 큐, 별도 worker, 관리자 권한, 실행·실패 기록 |
| 방어 조치 | 비밀번호 Argon2id 해시, 토큰 해시 저장·만료·폐기, 본인 범위 조회, 입력·본문 크기 제한, CORS, 요청 횟수 제한 |

신청 처리는 운영기관의 공식 원문에서 합니다. 이 서버의 찜은 신청 완료를 의미하지 않습니다.
학교 학생 인증·학교 SSO·푸시·챗봇·첨부파일 OCR·프론트엔드는 포함하지 않습니다.

## 3. 기본 모드와 운영 모드는 다릅니다

| 항목 | 로컬 기본값 | 실제 운영 전 필요한 설정 |
| --- | --- | --- |
| DB | SQLite 파일 | PostgreSQL 연결·접근 제어·백업/복구 검증 |
| 인증 | 서버 자체 아이디 인증 + DB 세션 | 팀에서 방식 합의, HTTPS, 프론트 인증 흐름 연결 |
| 분석 | `AI_PROVIDER=rules` | AI 사용 시 모델·키·비용·개인정보 처리 확인 |
| 수집 | `CRAWL_ENABLED=false` | 저장소 허가 기록 확인 후 승인된 조건 반영 |
| 자동 수집 | 꺼짐 | 합의된 주기와 학교의 제한 반영 |
| 실행 | API+worker 로컬 프로세스 | 프로세스 관리, 프록시, HTTPS, 모니터링 |

`APP_ENV=production`은 SQLite·HTTP 프론트 주소를 거부합니다.

이 구현은 **Supabase Auth 토큰이나 JWT를 받지 않습니다**.
자체 로그인에서 발급한 내용을 해석할 수 없는 무작위 Bearer 세션 토큰을 사용합니다.
Supabase를 선택할 때는 현재 구조에서는 PostgreSQL 호스팅 용도로만 연결합니다.
Supabase Auth를 사용하기로 합의하면 인증 계층과 프론트 계약을 별도로 변경해야 합니다.

## 4. 파일을 읽는 순서

| 문서 | 무엇을 설명하는가 |
| --- | --- |
| [GitHub 업로드 안내](docs/UPLOAD_GUIDE.md) | 정확히 어느 폴더를 올리는지, 브랜치와 PR, 올리면 안 되는 파일 |
| [처음 실행·API 시험](docs/LOCAL_TEST_GUIDE.md) | Windows 기준 실행 → Swagger 가입/로그인 → 프로필 저장 |
| [프론트 연동용 API 명세](docs/API_CONTRACT.md) | 프론트 담당자용 경로·인증·날짜·예외 규칙 |
| [openapi.json](docs/openapi.json) | 이 코드에서 생성한 기계 판독용 전체 API 명세 |
| [구조와 담당 파일](docs/ARCHITECTURE.md) | 코드 역할·DB·인증·추천 구조 |
| [공지 수집·분석 안내](docs/CRAWLER_AND_AI.md) | 실제 수집 활성화, 결과 확인, AI 전환, 수동 검수 |
| [배포 전 설정](docs/DEPLOYMENT.md) | PostgreSQL·Docker·학교 서버 연결 시 점검 |
| [기획 요구사항 대응표](docs/REQUIREMENTS_TRACEABILITY.md) | 저장소 기획 범위와 구현·시험 대응 |
| [검증 보고서](docs/TEST_REPORT.md) | 실제 실행한 검증과 아직 실행하지 못한 검증 |
| [전체 파일 목록](docs/FILE_MANIFEST.md) | 배포 ZIP의 전체 파일 목록 |

## 5. 팀에 먼저 공유할 결정 사항

현재 `config/recommendation-policy.json`은 분야 35 / 학과 25 / 학적 15 / 학년 10 / 활동 15의 제안값입니다.
시간 요소는 가산하지 않습니다. 확인되지 않은 자격 조건에는 일치 점수를 주지 않습니다.
임박 기간은 화면에서 `closing_days`로 명시해 요청합니다. 설정 파일의 7일은 화면 설계를 위한 제안입니다.

관심사 15개는 `app/reference.py`의 제안 사전입니다.
학과·학적 입력, 분야와 활동을 각각 하나 이상 선택해야 추천 API가 동작합니다.
별도 토큰 갱신 API는 없으며 기본 세션 유효기간은 24시간입니다.

루트 `docs/api-contract.md`에 React 연동 계약이 정리되어 있습니다.
프론트는 실제 API를 사용하며, 토큰은 탭의 sessionStorage에 저장합니다.
공지 카테고리는 반복 쿼리로 복수 선택할 수 있습니다.

## 6. 문서·실행 수정 통합 안내

이 폴더에는 이전 첫 실행 수정본이 이미 반영되어 있습니다. 별도 수정 ZIP을 다시 적용하지 않습니다.
Markdown 문서는 한국어로 정리했으며, 파일명·명령어·API 경로·JSON 필드명은 코드와 맞추기 위해 유지했습니다.
`docs/STARTUP_FIX.md`는 적용해야 할 추가 패치가 아니라 이미 반영된 수정 이력입니다.

커밋 제목·본문·PR 예시는 [커밋과 PR 작성 안내](docs/COMMIT_GUIDE.md)에 있습니다.
기존 개인 `.env`, `data/`, 팀원이 수정한 소스는 삭제하지 말고 차이를 비교한 뒤 적용합니다.
오래된 `.venv`만 따로 치우고 다시 만들 수 있으며, 가상환경 자체는 배포 ZIP에 포함하지 않습니다.

## 7. 테스트

테스트 도구까지 준비하려면 서버를 종료한 뒤:

```powershell
python start.py --dev --setup-only
.venv\Scripts\python.exe -m pytest
```

macOS/Linux에서는 `.venv/bin/python -m pytest`를 사용합니다.
테스트는 임시 DB·명시적인 합성 HTML을 사용하며 학교에 네트워크 요청을 보내지 않습니다.
실행 결과와 미검증 항목은 `docs/TEST_REPORT.md`에서 확인합니다.

실제 HTTP 흐름까지 검사하려면 아래 명령을 별도로 실행합니다.

```powershell
.venv\Scripts\python.exe scripts/verify_backend.py
```

이 검증 도구는 임시 폴더에 별도 DB·합성 공지·시험 계정을 만들고 실제 API 프로세스를 실행합니다.
개발용 `.env`와 실제 DB를 읽거나 수정하지 않으며, 종료 시 임시 데이터를 정리합니다.
학교 수집·외부 AI 검증은 아니므로 해당 연결은 별도로 시험해야 합니다.
검증 과정과 한계는 [검증 보고서](docs/TEST_REPORT.md)를 확인하세요.
